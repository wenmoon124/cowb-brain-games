# Cloudflare Pages redirects — only routes that resolve to generated pages
# Root → default locale (en)
/ /en 302
# Locale-agnostic redirects for the landing page only (other routes not yet generated)
